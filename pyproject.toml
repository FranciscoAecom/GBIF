[project]
name = "gbif-pipelines"
version = "0.1.0"
description = "Pipelines bronze, silver e gold para dados publicos do GBIF."
readme = "AGENTS.md"
requires-python = ">=3.11"
dependencies = [
    "geopandas==1.1.1",
    "openpyxl==3.1.5",
    "pyogrio==0.12.1",
    "requests==2.32.3",
    "shapely==2.1.2",
]

[tool.uv]
package = false
