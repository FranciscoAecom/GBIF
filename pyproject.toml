[project]
name = "gbif-pipelines"
version = "0.1.0"
description = "Pipelines bronze, silver e gold para dados publicos do GBIF."
readme = "AGENTS.md"
requires-python = ">=3.11"
dependencies = [
    "openpyxl==3.1.5",
    "requests==2.32.3",
]

[tool.uv]
package = false

